#include <bits/stdc++.h>
#include <iomanip>
#include <vector>

using namespace std;

typedef long long ll;

int main() {
    int N;
    cin >> N;

    vector<ll> v(N);
    for (auto& x: v) cin >> x;
}
